<Window x:Class="UPRO.View.InformWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UPRO.View"
        mc:Ignorable="d" Width="300" Height="120"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Style="{StaticResource CustomWindowStyle}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#2C2C2C" BorderThickness="0">
            <TextBlock x:Name="TxbMessage" TextWrapping="Wrap" VerticalAlignment="Center" HorizontalAlignment="Center"
           FontWeight="Bold" Foreground="#F8F8F8" Background="#2C2C2C" Text=""/>
        </Border>
        <Button Grid.Row="1" x:Name="BtnClose" Content="Close" FontWeight="Bold" Click="BtnClose_Click"
                Style="{StaticResource ButtonStyle2}"/>
    </Grid>
</Window>
